services:
  - type: web
    name: newbie-recp-discord-bot
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python main.py"
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.4
      - fromGroup: newbie-bot-secrets # あなたが設定した環境グループ名
